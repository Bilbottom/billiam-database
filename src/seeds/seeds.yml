---
version: 2

# Treating these as seeds just so that the CSVs can be read in with the
# `dbt seed` command

seeds:
  - name: finances
    description: Item-level transaction history from 2018-01-18.
  - name: daily_tracker
    description: Work tracker history from 2019-04-23.
